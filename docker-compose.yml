services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html
    depends_on:
      - connect

  connect:
    image: nodered/node-red
    ports:
      - "1880:1880"
    volumes:
      - ./data:/data